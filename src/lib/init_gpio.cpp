#include "gpio_lib.h"
#include "init_gpio.h"

void init_pin(int pin, char mode)
{
  /*int* pin_num;
  pin_num = & pin;
  if(mode == 'r')
    {
      Gpio g1(pin_num);
      g1.read_data();
    }
  else if(mode == 'w')
    {
      Gpio g1(pin_num);
      g1.write_data();
    }
  else
    {
      // code here
    }
  */
}
